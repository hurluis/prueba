# Variables de entorno para el proyecto

# Base de datos
DATABASE_URL=sqlite:///./app.db
IS_DOCKER=false

# Seguridad
SESSION_SECRET_KEY=tu_clave_secreta_muy_segura_aqui_32_caracteres_minimo

# Google OAuth credentials
# IMPORTANTE: Reemplaza estos valores con tus credenciales de Google Cloud Console
# 1. Ve a https://console.cloud.google.com/
# 2. Crea un nuevo proyecto o selecciona uno existente
# 3. Habilita la API de Google+
# 4. Ve a "Credenciales" > "Crear credenciales" > "ID de cliente de OAuth 2.0"
# 5. Configura la pantalla de consentimiento
# 6. Agrega los URIs de redirección autorizados (ver GOOGLE_REDIRECT_URI abajo)
GOOGLE_CLIENT_ID=tu_client_id_aqui.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu_client_secret_aqui

# URI de redirección después de la autenticación de Google
# DEBE coincidir EXACTAMENTE con lo configurado en Google Cloud Console
# Para desarrollo local con Docker: http://localhost/auth/google/callback
# Para producción: http://tu-dominio.com/auth/google/callback o https://tu-dominio.com/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost/auth/google/callback

# URL base del frontend para redirecciones después del login
# Para desarrollo local con Docker: http://localhost
# Para producción: http://tu-dominio.com o https://tu-dominio.com
FRONTEND_BASE_URL=http://localhost